"peerDependencies": {
  "europa-core": "^{{europaVersion}}"
},
"devDependencies": {
  "@types/jasmine": "^4.0.3",
  "europa-build": "^{{europaVersion}}",
  "europa-core": "^{{europaVersion}}",
  "europa-environment-node": "^{{europaVersion}}",
  "europa-test-jasmine-reporter": "^{{europaVersion}}"
},
"scripts": {
  "build": "europa-build run-script build",
  "flint": "europa-build run-script flint",
  "format": "europa-build run-script format",
  "lint": "europa-build run-script lint",
  "test": "europa-build run-script test"
},
"engines": {
  "node": "^12.20.0 || >=14"
}
