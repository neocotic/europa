{
  "name": "europa",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.1.2",
    "eslint": "^4.18.2",
    "eslint-config-notninja": "^0.2.3",
    "karma": "^2.0.0",
    "karma-chai": "^0.1.0",
    "karma-cli": "^1.0.1",
    "karma-mocha": "^1.3.0",
    "lerna": "^2.9.0",
    "mocha": "^5.0.4"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "lint": "eslint .",
    "pretest": "npm run lint && npm run build",
    "test": "lerna exec --ignore europa-test -- karma start"
  },
  "engines": {
    "node": ">=6"
  },
  "private": true
}
